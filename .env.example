# PG Food App Configuration

# Server Port
PORT=3000

# MongoDB Connection URI (e.g. local or MongoDB Atlas)
# If omitted or failed to connect, it will fall back to local JSON storage
MONGODB_URI=mongodb://127.0.0.1:27017/pgfood

# JWT Secret for admin/user authentication
JWT_SECRET=supersecretpgfoodtoken123
